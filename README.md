# app-factory
This repo’s job is to:  generate new app project skeletons, standardize CLAUDE.md, standardize docs, standardize repo structure , standardize stack presets, standardize first-build workflows  Then use Claude Code inside that repo to build the factory.
